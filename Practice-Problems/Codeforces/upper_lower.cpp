#include<bits/stdc++.h>
using namespace std;

//isupper, islower, toupper, tolower, isdigit, isspace
int main() {
    char a;
    cin >> a;
    if (isupper(a)) {
        cout << "Upper Case\n";
    }
    if (islower(a)) {
        cout << "Lower case\n";
    }
}